package screens;

import javax.swing.*;
import java.awt.*;

public class TrainingPanel extends JPanel {

    public TrainingPanel(Dimension dim) {
        setSize(dim);
        setBackground(Color.CYAN);
    }
}
